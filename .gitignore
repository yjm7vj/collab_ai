node_modules/
dist/
.wrangler/

# Local secrets. Never commit these.
.dev.vars
.dev.vars.*
!.dev.vars.example
!.dev.vars.test

# worker-configuration.d.ts is generated by `wrangler types` but is committed
# on purpose: it holds no secrets, and a fresh clone typechecks without it
# having to be regenerated first.

.DS_Store
.idea/
*.log
